import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  previewContainer: {
    paddingRight: 20,
  },
  filmPreview: {
    width: 100,
    height: 100,
    borderRadius: 100,
    borderColor: "#e0dfc8",
    borderWidth: 2,
  },
});

export default styles;
